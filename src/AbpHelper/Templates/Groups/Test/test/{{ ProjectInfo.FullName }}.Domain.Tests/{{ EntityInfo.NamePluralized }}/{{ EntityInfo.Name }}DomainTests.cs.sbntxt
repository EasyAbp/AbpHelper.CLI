using System.Threading.Tasks;
using Shouldly;
using Xunit;

namespace {{ ProjectInfo.FullName }}.{{ EntityInfo.NamePluralized }}
{
    public class {{ EntityInfo.Name }}DomainTests : {{ ProjectInfo.FullName }}DomainTestBase
    {
        public {{ EntityInfo.Name }}DomainTests()
        {
        }

        [Fact]
        public async Task Test1()
        {
            // Arrange

            // Assert

            // Assert
        }
    }
}
