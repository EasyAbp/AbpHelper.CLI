using System;
using Volo.Abp.Application.Dtos;

namespace {{ ProjectInfo.FullName }}.{{ EntityInfo.ClassName }}s.Dtos
{
    public class {{ EntityInfo.ClassName }}Dto : {{ EntityInfo.BaseType | string.replace "AggregateRoot" "Entity"}}Dto{{ if EntityInfo.PrimaryKey }}<{{ EntityInfo.PrimaryKey}}>{{ end }}
    {
        {{~ for prop in EntityInfo.Properties ~}}
        public {{ prop.Type}} {{ prop.Name }} {get; set;}

        {{~ end ~}}
    }
}